# DelonixOS — splash de arranque.
# O tema vive em /usr/share/plymouth/themes/delonix.
# Depois de alterar isto é preciso `mkinitcpio -P` (o build da ISO já o faz;
# num sistema instalado corre `sudo plymouth-set-default-theme -R delonix`).
[Daemon]
Theme=delonix
ShowDelay=0
DeviceTimeout=8
