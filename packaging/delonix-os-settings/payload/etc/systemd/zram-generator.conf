# DelonixOS — swap comprimida em RAM.
# Num posto que corre clusters locais (DKS/kind/minikube) e IDEs, o zram evita
# o OOM sem tocar no disco. zstd dá ~3:1 em heaps de Go/Java.
[zram0]
zram-size = min(ram / 2, 16384)
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
