#!/usr/bin/env bash
# delonix-update — the same as `delonix update`, for anything that is not the
# distro's zsh: scripts, cron, another shell.
#
# It exists as a real file on purpose: a shell function is no use to a systemd
# timer or to `ssh machine delonix-update`.
exec delonix-toolbox update "$@"
